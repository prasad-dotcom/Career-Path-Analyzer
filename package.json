{
  "name": "career-path-analyzer",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}